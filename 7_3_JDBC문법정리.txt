JDBC절차
1. JDBC드라이버 로드. 한번만
    Class.forName("com.mysql.cj.jdbc.Driver")
2. DB연결
    String url = "jdbc:mysql://localhost:3306/test";
    String user = "scoula";
    String password = "1234";
    Connection conn = null;
    conn = DriverManager.getConnection(url, user, password);

3. SQL송신 Statement, PreparedStatement 의 executeQuery("SELECT구문"); 또는 executeUpdate(DML구문);
4. SQL결과 수신 ResultSet
Statement stmt = conn.createStatement();
ResultSet rs =  stmt.executeQuery("SELECT name FROM t");
while(rs.next()){  String nameValue = rs.getString("name");   sop(nameValue); }

String newName = "test";
stmt.executeUpdate("INSERT INTO t VALUES( '" + newName  + "')"); //큰따옴표: java문자열구분자와 
                                                                                                 //작은따옴표 : sql문자열 구분자
                                                                                                 //구분 불편~
String insertSQL = "INSERT INTO t VALUES (?)";
PreparedStatement pstmt = conn.prepareStatement(insertSQL );
pstmt.setString(1, newName);
pstmt.executeUpdate();
     
     
5. DB연결 해제
rs.close();
stmt.close(); 또는 pstmt.close();
conn.close();